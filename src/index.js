import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(
  // <React.StrictMode> TODO: Reintroduce when Material UI 5 is released.
  <App />,
  // </React.StrictMode>,
  document.getElementById('root')
);
